import type { Student, Teacher, Subject, Class, Grade } from './types';

// These mocks are no longer the primary source of data for students, teachers, subjects, and classes.
// They are kept for reference or for pages not yet migrated to Firebase.
export const mockStudents: Student[] = [];

export const mockTeachers: Teacher[] = [];

export const mockSubjects: Subject[] = [];

export const mockClasses: Class[] = [];

export const mockGrades: Grade[] = [];
